site_name: aio-cache
site_description: Add your description here
site_url: https://github.com/owner/aio-cache

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            show_source: true

nav:
  - Home: index.md
  - API: api.md
